# Pick residues on specific chains
select highlight, (chain A and resi 165+201)

# Show rounder atoms (small spheres over sticks)
show sticks, highlight
show spheres, highlight
set sphere_scale, 0.3, highlight

# Make the sticks smoother & more refined
set stick_quality, 16, highlight
set stick_radius, 0.35, highlight

# Fade the rest
set cartoon_transparency, 0.8, (not highlight)
set cartoon_transparency, 0.0, highlight

# Outline effect for final rendering
set ray_trace_mode, 1
set ray_trace_gain, 0.1
set antialias, 2
